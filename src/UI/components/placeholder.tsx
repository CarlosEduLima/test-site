/* eslint-disable linebreak-style */
import React from 'react';

export const Placeholder = ({ label }: { label: string }) => {
  return (
    <div>
      Assim que adicionar algum arquivo nessa pasta, me exclua. Estou aqui como placeholder, para
      enviar a pasta para o GitHub.
      {label}
    </div>
  );
};
